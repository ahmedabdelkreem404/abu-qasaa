import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function DashboardInventoryPage() {
  return <PlaceholderPage title="Inventory" description="Branches, warehouses, stock items, and movement tracking placeholder." />;
}
