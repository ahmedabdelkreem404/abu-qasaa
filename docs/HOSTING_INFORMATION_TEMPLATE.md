# Hosting Information Template

Fill this document only with non-secret operational details. Never fill passwords, API keys, HMAC secrets, private keys, recovery codes, or payment credentials inside the committed document.

Secrets must be provided using the hosting control panel, server-only env files, or a secure secret manager.

```text
Hosting provider:
Server type:
Operating system:
SSH host:
SSH port:
SSH username:
Backend path:
Frontend path:
Frontend domain:
API domain:
Database host:
Database port:
Database name:
Database username:
PHP version:
Node.js version:
Process manager:
Mail provider:
Storage disk:
Backup destination:
Paymob mode:
```
